{
  "name": "bot-discord",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "db:migrate": "pnpm --filter shared prisma migrate dev",
    "db:generate": "pnpm --filter shared prisma generate",
    "db:push": "pnpm --filter shared prisma db push",
    "db:studio": "pnpm --filter shared prisma studio"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.15.0"
}
